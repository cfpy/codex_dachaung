# Matlab 参数到 Verilog 输入的量化与展平示例

下面用一个具体的数值例子演示如何把 `evaluatemnist.m` 中的矩阵参数（`x_valid`、`w1`、`w2`、`b1`、`b2`）转换为 `FP` 顶层模块可接收的定点总线输入。假设统一采用 8 位有符号定点、比例系数 \(S = 64\)（所有输入、权重、偏置都用同一个 \(S\)）。

## 1. 输入特征 `x_valid` → `input_data_all`
Matlab 一列特征：

```
x_valid(:,1) = [0.12; -0.05; 0.08; 0.00; 0.20];
```

量化到 8 位补码：`round(value * 64)` 并裁剪到 `[-128, 127]`，得到 `[8, -3, 5, 0, 13]`，再按索引从低到高打包到总线：

- 特征0 → `input_data_all[7:0]`   = 8'd8
- 特征1 → `input_data_all[15:8]` = 8'd253  (−3 的补码)
- 特征2 → `input_data_all[23:16]`= 8'd5
- 特征3 → `input_data_all[31:24]`= 8'd0
- 特征4 → `input_data_all[39:32]`= 8'd13

`Neuron` 会在第 *i* 次乘法取 `data_input[8*i +: 8]`，因此该顺序必须与 Matlab 列索引一致。

## 2. 隐藏层权重 `w1` → `weight_input_to_hidden`
假设隐藏层有 2 个神经元，`w1` 形状为 (2×5)：

```
w1 = [ 0.10  -0.02  0.05  0.00  0.08;
      -0.06   0.04  0.01  0.02 -0.03];
```

量化：乘以 64 并取整、裁剪 →
- 行0（隐藏0）：`[6, -1, 3, 0, 5]`
- 行1（隐藏1）：`[-4, 3, 1, 1, -2]`

展平到总线，按“神经元索引递增、行内按输入索引递增”的顺序拼接：

- 隐藏0权重 5×8 位片段占 `weight_input_to_hidden[39:0]`（从低位开始依次放 6、-1、3、0、5）
- 隐藏1权重 5×8 位片段紧随其后占 `weight_input_to_hidden[79:40]`（依次放 -4、3、1、1、-2）

`Layer` 会按神经元索引切出每个 40 位片段传给对应 `Neuron`，片内再按输入索引逐段乘法，与 Matlab 的 `w1 * x_valid + b1` 顺序一致。

## 3. 隐藏层偏置 `b1` → `bias_hidden`
示例偏置：`b1 = [0.02; -0.01]`。

量化后得到 `[1, -1]`，按神经元索引从低到高拼接：

- 偏置0 → `bias_hidden[7:0]`   = 8'd1
- 偏置1 → `bias_hidden[15:8]` = 8'd255 (−1 的补码)

## 4. 输出层权重 `w2` → `weight_hidden_to_output`
假设输出层 1 个神经元，`w2` 形状为 (1×2)：

```
w2 = [0.07  -0.05];
```

量化得到 `[4, -3]`。按输出神经元索引展平：

- 输出0 的 2×8 位片段从最低位开始放入 `weight_hidden_to_output[15:0]`（依次 4、-3）。

若有多个输出神经元，下一行（下一神经元）的量化结果紧随其后拼接即可。

## 5. 输出层偏置 `b2` → `bias_output`
示例：`b2 = [0.01]` → 量化后 `[1]`，直接放入 `bias_output[7:0]`。多输出时按神经元索引递增依次放置。

---

按照上述规则——统一缩放、行（神经元）优先、行内列索引递增展平——即可把 Matlab 的矩阵/向量参数转成 `FP` 所需的宽总线定点输入，并保持与 `evaluatemnist.m` 的乘加顺序一致。
